# ctools

> A js command tool

## How to use

``` bash
# install ctools
npm install ctools -g

# ctools push
 ## dev push
 push
 push --push
 push --not-publish


 ## issues branch push
 push
 push --todev
 push --publish

```
